allprojects {
    group 'dev.dhdf'
    version '1.0.0'

    ext {
        slf4jVersion = '1.7.30'
        adventureVersion = '4.3.0'
    }
}

subprojects {
    apply plugin: 'java'

    sourceCompatibility = 8
    targetCompatibility = 8
}
